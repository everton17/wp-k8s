apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-wordpress-service
spec:
  selector:
    app: {{ .Release.Name }}-wordpress
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP